class Solution {
    class Cell {
        int row, col, elevation;

        Cell(int row, int col, int elevation) {
            this.row = row;
            this.col = col;
            this.elevation = elevation;
        }
    }
}
