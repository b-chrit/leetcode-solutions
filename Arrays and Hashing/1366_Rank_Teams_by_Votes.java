class Solution {
    class Team {
        char name;
        int[] votes;
        
        Team(char name, int positions) {
            this.name = name;
            this.votes = new int[positions];
        }
    }
}
